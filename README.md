# TMDC

Vercel-ready landing page with:

- Static frontend built from `frontend/`
- Serverless FastAPI endpoint at `/api/*`
- Google Sheets webhook lead capture via `GOOGLE_SHEETS_WEBHOOK_URL`

Deploy the project root to Vercel and set `GOOGLE_SHEETS_WEBHOOK_URL` in the project environment variables.
